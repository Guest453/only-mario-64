# Copy to arena/.env on the VM and fill in. NEVER commit the real .env —
# only-mario-64 is a public repo and GitHub secret-scanning will report a
# committed client secret straight to Discord, which auto-revokes it.
DISCORD_CLIENT_ID=1497465820182216815
DISCORD_CLIENT_SECRET=replace-me
ARENA_HOST_TOKEN=generate-with-openssl-rand-hex-32
ARENA_ADMIN_ID=1246945967102623755
